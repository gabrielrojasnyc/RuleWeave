// Note: We've moved the API client logic entirely to the server
// This file now only has helper functions for the frontend

// These are just dummy function signatures
// The actual API calls will be made from the API routes
export const translateRuleToCode = async (naturalLanguageRule, apiKey) => {
  // This is just a placeholder - the real function is in the API route
  throw new Error('This function should not be called directly from the frontend');
};

export const validateRule = async (ruleCode, apiKey) => {
  // This is just a placeholder - the real function is in the API route
  throw new Error('This function should not be called directly from the frontend');
};